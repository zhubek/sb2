services:
  db:
    image: postgres:16-alpine
    container_name: sb2-postgres
    environment:
      POSTGRES_USER: sb2
      POSTGRES_PASSWORD: sb2dev
      POSTGRES_DB: sb2
    ports:
      - "5434:5432"
    volumes:
      - sb2-pgdata:/var/lib/postgresql/data

volumes:
  sb2-pgdata:
