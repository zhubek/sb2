"use client";
import { useState } from "react";
import { Download, Upload } from "lucide-react";
export default function Settings(){const [file,setFile]=useState<File|null>(null),[message,setMessage]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);return <><div className="admin-eyebrow">УПРАВЛЕНИЕ</div><h1 className="admin-title">Резервная копия контента</h1><p className="admin-subtitle">Сохраняйте копию материалов перед большими изменениями или переносом платформы.</p><div className="admin-card" style={{padding:28,marginTop:28}}><h2 className="font-semibold">Экспорт материалов</h2><p className="admin-subtitle">Копия содержит все сохранённые черновики, публикации и историю изменений. Личные ответы учеников в неё не входят.</p><a className="admin-btn primary" style={{marginTop:20}} href="/api/admin/backup" download><Download size={14}/>Скачать резервную копию</a></div><form className="admin-card" style={{padding:28,marginTop:22}} onSubmit={async e=>{e.preventDefault();if(!file)return;setBusy(true);setError('');setMessage('');try{const r=await fetch('/api/admin/backup',{method:'POST',headers:{'Content-Type':'application/json'},body:await file.text()});const d=await r.json();if(!r.ok)throw new Error(d.error);setMessage(`Восстановлено материалов: ${d.count}. Проверьте черновики перед публикацией.`);}catch(e){setError((e as Error).message);}finally{setBusy(false);}}}><h2 className="font-semibold">Восстановить из копии</h2><p className="admin-subtitle">Материалы из файла заменят соответствующие черновики. Опубликованный сайт останется доступен в текущем виде. Для применения изменений откройте и опубликуйте нужные материалы.</p><label className="admin-field" style={{marginTop:24}}><span>Файл резервной копии (.json, до 25 МБ)</span><input type="file" accept=".json,application/json" onChange={e=>setFile(e.target.files?.[0]||null)} required/></label><button className="admin-btn" disabled={!file||busy} style={{marginTop:20}}><Upload size={14}/>{busy?'Восстанавливаем…':'Восстановить в черновики'}</button>{message&&<p className="admin-success" role="status" style={{marginTop:20}}>{message}</p>}{error&&<div className="admin-error" role="alert">{error}</div>}</form></>;}
