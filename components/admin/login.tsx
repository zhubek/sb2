"use client";
import { useState } from "react";
import { ArrowRight, LockKeyhole } from "lucide-react";
import { LogoMark } from "@/components/compass-marks";
export default function AdminLogin({local}:{local:boolean}) {
  const [password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
  return <div className="admin-app admin-login"><form className="admin-login-card" onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await fetch('/api/admin/session',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password})});const d=await r.json();if(!r.ok)throw new Error(d.error);window.location.reload();}catch(e){setError((e as Error).message);setBusy(false);}}}><div className="flex items-center gap-3"><LogoMark className="h-9 w-9 text-violet-500"/><strong>Smart Bolashaq</strong></div><a href="/login" className="admin-btn" style={{marginTop:20}}>Вход по почте и паролю →</a><h1>Ваш контент.<br/>Под вашим контролем.</h1><p>Управляйте тестами, страницами и образовательными материалами из одного места.</p>{!local && <label className="admin-field" style={{marginBottom:20}}><span>Пароль администратора</span><input className="admin-input" type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>}{error&&<div className="admin-error" role="alert">{error}</div>}<button className="admin-btn primary" disabled={busy}>{busy?'Входим…':local?'Войти в локальную панель':'Войти'}<ArrowRight size={15}/></button><div className="flex items-center gap-2" style={{color:'#a2a4b3',fontSize:10,marginTop:22}}><LockKeyhole size={12}/>{local?'Локальная среда разработки':'Доступ только для администратора'}</div></form></div>;
}
